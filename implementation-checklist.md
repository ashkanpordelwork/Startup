# چک‌لیست زنده پیاده‌سازی

> برنچ: `claude/redesign-v2` — این فایل بعد از هر قدم آپدیت می‌شود.

وضعیت‌ها: ⏳ در انتظار / 🔨 در حال انجام / 👀 آماده‌ی بررسی / ✅ تایید شده

## مرحله ۰ — پایه‌ی سیستم طراحی
- [x] ✅ توکن‌های طراحی (رنگ مونوکروم، فونت IRANYekan، افکت شیشه‌ای) در Tailwind/CSS — تایید شد
- [x] ✅ آیکون‌های reicon.dev — بررسی شد: تب‌بار از قبل درست بود (outline/filled بر اساس فعال بودن)، بدون ایموجی در کد واقعی
- [x] 👀 اصلاح Home.tsx (حذف ۲ ایموجی باقی‌مانده در کل کدبیس، کارت شیشه‌ای واقعی) — منتظر تایید تو
- [x] 👀 فیچر واقعی «کارت چک‌این روزانه» (بک‌اند + فرانت) — ساخته شد: `GET /me/today` (همه‌ی اقدام‌های فعال از همه‌ی پلن‌ها) + کارت شیشه‌ای تیره روی Home با گزارش سریع — منتظر تایید تو

## مرحله ۱ — فلوی اونبوردینگ
- [x] 👀 استایل جدید روی صفحه‌ی چت موجود (حباب مشکی/شیشه‌ای، نوار ورودی شیشه‌ای) — منتظر تایید تو
- [ ] ⏳ **نیاز به تصمیم تو:** چت فعلی هنوز فرم ساختاریافته‌ی قدیمیه (سوال بله/خیر پشت‌سرهم)، نه فلوی «متن آزاد + AI واقعی» که طراحی کردیم. برای ساختن نسخه‌ی واقعی، به یه **کلید API از AvalAI** نیاز دارم (حساب بسازی، کلید API بگیری، همینجا بفرستی)
- [x] 👀 اتصال AI (DeepSeek از طریق AvalAI) — کد کامل نوشته و build شد؛ تست زنده امکان‌پذیر نیست چون avalai.ir از Sandbox من مسدوده. کلید امن ذخیره شده (فقط .env، commit نشده)
- [x] ✅ منطق پرچم قرمز با اولویت — بررسی شد: از قبل درست بود (۶ سوال پرچم قرمز همیشه اول پرسیده می‌شن)
- [x] ✅ نوار پیشرفت — از قبل وجود داشت
- [x] 👀 توقف/ادامه — ساخته شد (ذخیره‌ی خودکار پیشرفت در localStorage، پاک‌شدن بعد از تایید نهایی پلن) — منتظر تایید تو
- [x] ✅ اتصال به موتور قانون‌محور موجود برای تولید پلن — بررسی شد: از قبل وصل بود (`intake.ts` مستقیماً `computeTrack` را صدا می‌زند)

**فلوی اونبوردینگ کامل شد** (منتظر تایید نهایی تو روی موارد 👀)

## مراحل بعدی (هنوز شکسته نشده به قدم‌های ریز)
- [x] 👀 چک‌این روزانه — کارت مرکزی + تشخیص الگو (۳ بار پشت‌سرهم «سخته») + صفحه‌ی پیشنهاد با تایید صریح (فعلاً توی تب نوتیفیکیشن، چون سیستم نوتیفیکیشن کامل هنوز ساخته نشده) — منتظر تایید تو
  - **اصلاح مهم:** رفتار قبلی که با اولین «سخته» بی‌صدا اقدام رو عوض می‌کرد، حذف شد (مغایر با تصمیم محصول بود)
- [ ] ⏳ اشتراک/شارژ + محدودیت مصرف
- [ ] ⏳ نوتیفیکیشن
- [ ] ⏳ پروفایل
- [ ] ⏳ فارغ‌التحصیلی و پلن بعدی
- [ ] ⏳ تریاژ مجدد رسمی

## بازخورد کاربر بعد از تست زنده (رفع شد)
- [x] ✅ حذف کارت «هماهنگی با پزشک» که همیشه توی Track قرمز نشون داده می‌شد — عنوان پلن و اقدام‌ها هم اصلاح شد تا محور «پزشک» نباشه
- [x] ✅ سوال بارداری فقط اگه جنسیت «زن» انتخاب بشه پرسیده می‌شه (سوال جنسیت به ابتدای فلو اضافه شد)
- [x] 👀 کل پنل سوالات از داخل اسکرول چت بیرون اومد و به یه **بات‌شیت ثابت** در پایین صفحه تبدیل شد (شبیه الگوی سوال‌پرسیدن خود Claude) — دیگه شبیه حباب‌های چت پشت‌سرهم نیست، یه پنل واحده که فقط محتواش عوض می‌شه — منتظر تایید تو
- تمام تست‌های واحد بک‌اند (۳۷ تست) بعد از این تغییرات همچنان سبزن

## اتصال AI واقعی (در حال انجام)
- [x] 👀 **کاوش آزاد برای ورودی مبهم**: وقتی متن اول کاربر به هیچ کلیدواژه‌ای نمی‌خوره (مثلاً «نمی‌دونم چمه، فقط حالم خوب نیست»)، AI (DeepSeek) یک سوال کنجکاوانه‌ی واقعی می‌سازه و می‌پرسه، به‌جای رفتن مستقیم سراغ فرم — منتظر تایید تو (نیاز به `AI_PROVIDER=avalai` و کلید معتبر برای تست زنده)
- [ ] ⏳ لحن طبیعی برای پرسیدن خود ۱۹ سوال بانک (فعلاً متن ثابت هر سوال عیناً نمایش داده می‌شه)
- [ ] ⏳ گفتگوی موضوعی (سوال درباره‌ی یک اقدام) — از قبل به AiProvider وصله (`answerQuestion`)، نیاز به تست زنده
- [ ] ⏳ پیشنهاد تطبیق (`/me/suggestions`) — از قبل به AiProvider وصله (`adaptAction`)، نیاز به تست زنده
