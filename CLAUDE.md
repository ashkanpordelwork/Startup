# دستورالعمل کار برای Claude در این ریپو

## قانون تداوم کار (خیلی مهم)

کارفرما (صاحب پروژه) این قانون رو صریحاً خواسته و باید همیشه رعایت بشه:

- اگر وسط انجام یک کار، کاربر به هر دلیلی از سیستم Claude خارج بشه (قطعی، بستن تب، هر چیز دیگه)، باید کار رو خودت تا انتها ادامه بدی و تمومش کنی. منتظر تایید کاربر برای ادامه‌ی مراحل بعدی نمون، مگر اینکه تصمیم واقعاً به قضاوت محصولی کاربر نیاز داشته باشه (نه یک جزئیات پیاده‌سازی).
- اگر کاربر دیس‌کانکت بشه، همین‌طور: کار رو خودت جلو ببر.
- اگر توکن‌ها به لیمیت خورد و کاربر در دسترس نبود، به محض اینکه لیمیت باز شد و امکان ادامه فراهم شد، خودت به‌صورت خودکار کار رو از همون‌جا که مونده بود از سر بگیر — منتظر پیام جدید از کاربر نمون.
- خلاصه: پیش‌فرض این پروژه "اجرای خودکار و تا انتها" هست، نه "توقف و پرسیدن" — مگر در تصمیمات محصولی/طراحی بزرگ که واقعاً باید کاربر (که خودش طراح محصول است) نظر بده.

## نکات مهم دیگر درباره‌ی این پروژه

- کاربر خودش **طراح محصول و UX** است؛ کیفیت UX خط قرمزشه. بعد از هر تسک UI/UX، به‌عنوان یک طراح حرفه‌ای کل فلو رو خودت بازبینی (self-QA) کن، مشکلات رو پیدا و اصلاح کن، و در آخر اسکرین‌شات بفرست.
- این اپ **مشاوره‌ای نیست** — هدف کمک به تغییر واقعی زندگی کاربره. سیستم نباید راه فرار از سختی (حذف کردن، بی‌خیال شدن) بده؛ همیشه باید مسیر رو ساده‌تر/قابل‌انجام‌تر کنه، نه اینکه هدف رو کنار بذاره.
- هیچ LLM واقعی وصل نیست — همه‌چیز rule-based هست، اما زیرساخت (`backend/src/ai/`) طوری طراحی شده که وصل کردن یک AI واقعی در آینده فقط نیاز به نوشتن یک provider جدید داره، نه تغییر بقیه‌ی کد.
- محیط اجرا گاهی ری‌استارت می‌شه و working directory محلی به یک کامیت قدیمی برمی‌گرده. همیشه اول `git status` و `git log --oneline -3` رو چک کن؛ اگر HEAD عقب‌تر از `origin/<branch>` بود، با `git fetch` + `git merge --ff-only origin/<branch>` سینک کن قبل از هر کار دیگه‌ای.
- شاخه‌ی کاری: `claude/health-chatbot-mvp-triage-41ojkn`.
